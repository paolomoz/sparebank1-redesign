# Canon requests (workers → lead)

Append-only. Each request names the slug that needed it, what was solved locally (with `data-deviation`) and what the lead may lift into `chrome.mjs` / `data.mjs` / the checker.

## Worker C — 2026-09-15

1. **`.badge` (DESIGN.md § Badges: pill, Frost-30 fill, Fjell text, Label 13 medium).** Needed by `nb-bank-om-oss-nyheter-bankkort-laget-av-resirkulert-plast-html` ("Relaterte tema" `#Bærekraft` ×2) and by every listing that will render tema filters (dynamics #11). Solved locally in the article page CSS (`a.badge`, min-height 40, hover Vann/Hvit) with `data-deviation`. Suggest lifting the rule into `cssBase()`.
2. **Frontend-clientlib footer builder.** `footerData()` matches only the standard footer (`.footer-bottom__*`). The 12 `nettsider-frontend` pages carry `main > footer.footer` with `.footer-columns__top .footer-columns__column` / `.footer-columns__bottom li a` / `.footer-info`, and social anchors with no text (the name is in `title`). `frontendFooter(main)` (exported from the article page module, duplicated in the listing module) returns the `footerData()` shape with `tabs: []` — suggest moving it into `data.mjs` and letting `build.mjs` fall back to it when `footerData()` returns empty columns.
3. **Lazy footer icons on standard pages.** On `nb-bank-privat-sparing-markedsnytt-html` the footer social icons are `<img data-lazy-src>` with no `src`, so `footerData()` yields `icon: null` and the anchors have no name. Solved with `patchData()` in the page module (fills `icon` from `data-lazy-src` and `t` from the anchor `title`). Suggest `footerData()` read `src || data-lazy-src || data-lazy-largesrc` and fall back to `title` for the link text.
4. **Header "Bli kunde" on frontend-clientlib pages.** Captured as `<button class="ffe-button--action">Bli kunde</button>` (no href) inside `#login .bank-buttons`, so `headerData()` returns `bliKunde: null` and the om-oss news header renders without it. Not invented locally. Lead decision: wire the canonical `/nb/bank/privat/kundeservice/bestill/bli-kunde.html` href in `headerData()` when the button is present.
5. **Content-checker logo regex.** `/logo\.svg|chevron\.svg|material-icons/` excludes the canon page's `logo.svg` but not the om-oss pages' `logo-sparebank1.svg` (rendered inline by `logoSvg()`); every om-oss / frontend page therefore reports 1–2 "missing" images. Suggest `/logo[a-z0-9-]*\.svg/`. Not weakened locally.
6. **Validator: YouTube embeds.** `validate-prototype.mjs` aborts every non-sparebank1 request, so a captured-src `<iframe src="https://www.youtube.com/embed/…" loading="lazy">` (ARCHETYPE-BRIEF rule 5) is a P1 network failure. On markedsnytt all three embeds render as linked static 16:9 frames (`a.video-frame.video-link`, `data-deviation`) — suggest the validator fulfil `youtube.com/embed` with a plate (counted as externalFulfilled) so captured-src embeds can ship as iframes; and a canon `.video-frame` + play glyph (`icons.play`) since two of the captured embeds have runtime-injected src and will need the linked frame anyway.
7. **Feedback strip labels.** The captured feedback buttons carry their labels only as `<svg><title>Ja</title>` / `Nei`; the canon `.feedback` recipe in ARCHETYPE-BRIEF rule 4 names the thumb icons but not the label treatment. Presse and markedsnytt render `<button type="button" class="btn btn-secondary">{icon}{label}</button>` — suggest recording this as the canon feedback markup.
## Worker A — 2026-09-14 (slugs: boliglan, lan hub, om-oss/hjemme, bedrift borettslag-sameie)

1. **Lazy footer social icons** (`data-lazy-src` only on `nb-bank-privat-lan-boliglan-html`) — same as Worker C #3; solved locally with `patchData()` in each of my four page modules.
2. **Header "Bli kunde" on `nettsider-frontend` pages.** On the clientlib pages (e.g. `nb-bank-om-oss-hjemme-html`) the captured header carries `<button class="ffe-button--action">Bli kunde</button>` with no href (JS handler), so `headerData().bliKunde` is null and the canon header renders only Logg inn. Request: fall back to the site-wide Bli kunde URL (`/nb/bank/privat/kundeservice/bestill/bli-kunde.html`, present in the same page's footer) when the button has no href. Not solved locally (chrome). — worker A
3. **content-check.mjs img exclusion regex vs the clientlib logo.** The in-main header of `nettsider-frontend` pages carries `<img src="/content/dam/SB1/ikoner/GUI-ikoner/logo-sparebank1.svg">`; the checker excludes `logo\.svg|chevron\.svg|material-icons` only, so `logo-sparebank1.svg` is flagged (header + main regions) although the canon header renders the logo as inline SVG. Request: extend the regex to `logo(-sparebank1)?\.svg` (or exclude `.header__logo img`). Legitimate exclusion recorded in the hjemme shape brief; the gate is otherwise PASS (0 text / 0 hrefs missing). — worker A
- **Callout info variant.** `.callout` ships only `icons.bulb` (Sol) — a tip. Sperre-kort's "Er kortet ditt misbrukt?" is a captured `ffe-message-box--info` (fraud branch), not a tip; an info glyph variant (`.callout.info` + `icons.info`) would fit the captured semantics (finish reviewer, sperre-kort round 1 item 5).
- **Anchorable contact row.** Task pages say "kontakt oss på telefon"; the canon contact section has no `id`, so the sentence cannot link to it. Suggest `id="kontakt"` on `section.contact` (chrome) so page modules may wrap that sentence's captured text in an in-page link (href only, no text edit) — reviewer item 6.
- **Bedrift header/footer variant gaps (content-check).** On `nb-bank-bedrift-…-fast-flytende-rente-bedriftslan-html` the checker flags three chrome items the page module cannot reach: (a) header `a.goto-btn.to-forsikring` "Til forsikring" → https://kundeforsikring.sparebank1.no is a visible link in the bedrift login cluster (privat renders the cluster hidden), `headerHtml` renders only "Logg inn"; (b) footer "Ring oss" panel: `top.info` "Ring fra utland +47 915 02300" is captured as a `tel:+4791502300` link, `footerData()` keeps text only → href lost; (c) bedrift logo img is `logo-sparebank1.svg` (checker excludes `logo\.svg` only; canon renders the inline SVG with the captured alt — an exclusion pattern fix in the checker or lead decision). Main + bank-choice regions PASS.
4. **Bedrift header: "Til forsikring" login-cluster link.** The bedrift variant (e.g. `nb-bank-bedrift-bedriftsforsikring-bransjer-borettslag-sameie-html`) carries `<a id="forsikring-button" class="goto-btn to-forsikring login-buttons" href="https://kundeforsikring.sparebank1.no">Til forsikring</a>` inside `.menu-actions .user-buttons`; `headerData()` has no slot for it, so the href is lost and content-check flags it in the header region. Request: capture `.user-buttons a[href]` extras into `header.extraLinks[]` and render them as `.btn-icon`/secondary text pills beside Logg inn. — worker A
5. **Footer "Ring fra utland" tel link in the top block.** `footerData()` reads `.call-banks__col2 .call-option__sub-text` as plain text (`panel.top.info[]`), so the captured `<a href="tel:+4791502300">+47 915 02300</a>` loses its href (flagged as missing `tel:+4791502300`). Request: keep the sub-number as `{t, tel}` and render it as a link (as `bank-numbers` already does for per-bank abroad numbers). — worker A
- **`.title` class missing from canon CSS.** DESIGN.md and ARCHETYPE-BRIEF list `.title` among the type classes, but `cssBase()` ships only `.display`, `.title-sm`, `.lead`, `.small`, `.label`; an `h2.title` renders at headline-sm. Hub and kontakt define it locally (`.title{font-family:var(--heading-font-family);font-size:var(--t-title);line-height:1.2}`) — worth adding to canon.
- **Router open state at 768 overflows.** With `details.router-all` open at 768, `li`, `a`, `span.bank-tagline` in `.bank-list` measure ~823 px against the 768 viewport (harness `wide` list on the FAQ page, where the smoke opens the router) — clipped, not wrapped. Canon CSS (`.bank-list` grid `minmax(220px,1fr)` inside the `all` grid area at ≤ 1023). Finish reviewer, FAQ round 1 item 4.
- **Harness smoke vs. router-in-main.** `validate-prototype.mjs` clicks the first `main details` before the full-page screenshot; pages that must render the router inside `<main>` (FAQ, until the art is eager in canon) get open-bank-list captures. Suggest scoping the smoke to `main article details, main section details`. Closed-state captures for the FAQ page are at `stardust/validation/<slug>/{1440,768,390}-closed.png`.
6. **`footerHtml()` duplicates the "Melde skade" panel heading.** In the Ring oss panel the top block renders `<h3 class="panel-title">Melde skade <a>915 02300</a></h3>` and then the guard `if(p.heading && p.heading!==t.name && !body.includes(esc(p.heading)))` prepends a second `<h3>` with `p.heading` ("Melde skade 915 02300" — the number is inside the link so the substring test fails). Present on the canon page and on every page with the standard footer (flagged by the finish reviewer on `nb-bank-bedrift-bedriftsforsikring-bransjer-borettslag-sameie-html`). Request: compare against `norm(p.top.heading+' '+p.top.number)` or skip the fallback when `p.top` exists. Chrome — not solved locally. — worker A
