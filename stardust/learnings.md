# Learnings — sparebank1-redesign (Flow B) migrate + rollout

One entry per failure class this run surfaced (skills/stardust/reference/learnings.md format). `status: pending` = to be harvested by the plugin maintainers.

| # | class | evidence | proposed skill change | status |
|---|---|---|---|---|
| L1 | zsh word-splitting | `node migrate.mjs $SLUGS` (70 slugs) reached the driver as ONE argument → "0 pages in scope"; `${=SLUGS}` fixed it | deploy/rollout SKILL § batched delivery: note `${=VAR}` / `xargs` for slug lists under zsh (next to the existing zsh PATH gotcha) | pending |
| L2 | stale local servers | default ports 3010/8812 were held by a sibling project's servers; `curl` returned 200 from the WRONG project | deploy SKILL Step 10 already warns for the prototype port; extend to the emulation port and make `serve.mjs` refuse EADDRINUSE loudly instead of dying silently under `nohup` | pending |
| L3 | HTML answered as SVG | a wrong DAM path returns an HTML page with 200; the rasteriser rendered three identical 76 KB "SVG" PNGs | `rasterise-svg.mjs`: validate `<svg` in the response (done here); take URLs from the pages (`--from-migrated`) not from memory | pending |
| L4 | migrate fixtures unresolvable | `pagemap-audit.mjs` / `file-protocol-audit.mjs` import Playwright from the plugin dir; `?query` links and a `<meta refresh>` entry page read as failures | migrate SKILL Phase 3: copy the fixtures into the project (the extract § Setup rule) and strip `?query#hash` before resolving relative targets | pending |
| L5 | `p + p` rhythm loss | every canon movement lost 16–24 px when EDS wrapped each authored `<p>` in its own layout div (`p + p { margin-top }` never matches) | deploy SKILL § 8 (EW2): add "re-create sibling-margin rhythm on the WRAPPERS" to the wrapper-class rule; a probe line in `_probe-boxes` catches it in one pass | pending |
| L6 | tracking-flips-wrap | `h3 { letter-spacing: -0.005em }` vs the canon summary's normal tracking flipped a line break at 360 (−30 px) | replica/deploy gate docs: match `letter-spacing` exactly on text-bearing elements, not only family/size/weight | pending |
| L7 | `<picture>` shrink-wrap | the pipeline's `<picture>` inside a flex `figure` shrinks to the image's intrinsic size, so `img { width: min(100%, 400px) }` resolved to 300 px on the published origin only (emulation serves bare `<img>`) | deploy SKILL anti-pattern #36/#111 family: "`picture { display:block; width:100% }` in every media slot"; make the emulation wrap EVERY external `<img>` in `<picture>` like the pipeline | pending |
| L8 | edge-NBSP debris | captured `…Woll.&nbsp;</p>` renders an extra wrapped line at 360 in the prototype; the pipeline trims it → Δh 25/72 on the published origin only | deploy ENCODE contract #112 covers whitespace-only paragraphs; add "trailing/leading NBSP at block edges" and strip it at the prototype/migrate source (assemble.mjs here) | pending |
| L9 | variant name = canon compound | a block variant token `illu` matched `styles.css .illu { width:72px }` and collapsed the whole block | deploy SKILL Step 2 naming: reserve every foundation compound class name from the variant namespace (lint: variant token ∈ styles.css class list → 🔴) | pending |
| L10 | url() with parentheses | `url("…fremtind-(skal-kun-brukes…)-/x.jpg")` truncated by `url\(["']?([^"')]+)` → `about:error` after publish | extract/prototype helpers: parse quoted `url("…")` first; the deploy-batch `.plain.html` verify caught it (keep) | pending |
| L11 | group CSS import order | `@import`ed group files load BEFORE the block's own rules, so equal-specificity variant rules lose | if per-group files are used for parallel workers, import them AFTER the core rules (concatenate at build) — or document "variant rules need one extra class" | pending |
| L12 | worker stalls | two agents stopped mid-write (watchdog 600 s, API server error); resuming with "check disk, continue in small steps" recovered both without rework | stardust master SKILL § Hands-off: recommend writing large files in several edits and re-verifying archetype byte-identity after every step | pending |
| L13 | unresolved AEM link templates | captured hrefs `${links.x}` and an author's note as href ("Legg inn link til …") | migrate content-preservation § Internal link rewriting: name the class (keep verbatim in `data-unresolved-href`, bounce to the source page, flag) | pending |
| L14 | delivery-lint one-cta-per-p | fires on any `<strong>` inside a multi-link running-text paragraph (investor RTE) | rollout delivery-lint: only count links that are the sole content of a `<strong>/<em>` | pending |
| L15 | regional alternates in chrome | footer/router bank links are per-page regional alternates of the CURRENT page; a shared document cannot carry them | deploy Step 6 (chrome documents): add "per-page link targets in captured chrome → normalise to a stable target and record the decision" | pending |
