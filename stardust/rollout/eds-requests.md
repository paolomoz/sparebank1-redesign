
## story (faq · news-article · news-listing · campaign-landing) — Worker C

1. **Frontend footer renders the legal links as a fourth column.** `/footer-frontend-om-oss-2` (and by the same shape `/footer-frontend`, `/footer-frontend-2`, `/footer-frontend-om-oss`) paints at 1440: `footer` 733 px vs prototype 481 px — `.footer-main` 641 vs 389 because `.footer-cols` holds a 4th `div.footer-col` (y 5162, h 220) with the "Om SpareBank 1 / Vilkår for bruk av nettsiden / Personvern / Informasjonskapsler / For utviklere" list that the prototype renders only in `.footer-legal` (which EDS also renders, 92 px, correct). The list wraps to a second grid row (+252 px). Every frontend/story page therefore fails the footer crop (81 % at 1440, 77 % at 360) and Δh by −252 regardless of body fidelity (news-listing at 1440: pixel 2.23 %, Δh exactly −252). Measured on `http://localhost:3020/nb/bank/om-oss/nyheter` vs `http://localhost:8820/nb-bank-om-oss-nyheter-html-proposed.html`. Chrome — not touched. Request: the footer block (or the footer document's section split) must not treat the legal-links section as a column.

## theme + market-landing — EDS Worker E2
- **Core `.cards li.tile .card-title :is(h2, h3) { line-height: 1.2 }` vs canon h2 1.15** — measured on nb-bank-privat-html at 1440: proto `h2.tile-title` 34 px (29.44 px × 1.15), EDS 35 px; +1 px per tile row, +2 px on the 6-tile grid (685 vs 682). Worked around in `blocks/cards/cards-theme.css` (`.cards.tiles li.tile .card-title :is(h2,h3) { line-height: 1.15 }`) — please fold into cards.css and drop my override.
- **Core `.callout .callout-text p { margin: 0 }` drops the canon `p + p` 16 px** — measured on the theme callout (h2, p, CTA p): proto CTA at 201, EDS 185 (−16). Same trap in `.hero .hero-text p { margin: 0 }` (campaign note: proto 465 / EDS 449 at 360) and `.columns .col-text p { margin: 0 }` (promo band). Worked around per variant (`callout-theme.css` `.callout.offer`, `hero-theme.css` `.hero.campaign .lead-wrap + .note-wrap`); the core promo `.cta-row` margin already carries the 16+4. A general core rule (`.callout-text p + p`, `.hero-text .lead-wrap + .note-wrap`) would remove three family overrides.
- **Core `@media (≤640) .callout .callout { flex-direction: column }` has no canon counterpart** — canon.css keeps the callout a row at every width (theme proto at 360: 364 vs EDS 408, +44 = icon row + gap). Overridden for `.callout.offer` only; other groups' callouts will show the same +44 at 360.
- **Prototype defect (renderer `nb-bank-bedrift-bedriftsforsikring-bransjer-borettslag-sameie-html.mjs`)**: at ≤767 `.sheets` keeps `grid-template-rows: auto auto auto auto` while `.sheet { grid-row: auto }` — the two stacked sheets occupy rows 1–2 and the 2 empty tracks × 48 px gap leave 97 px of blank Sand before the compare link. `columns-theme.css` matches it for gate parity (`.columns.sheets .split-row` keeps `repeat(4, auto)` at 767, marked PROTOTYPE PARITY) — fix the renderer (`.sheets{grid-template-rows:none}` in the 767 query) and drop the parity line together.
- **`lib.inline` collapses ` ` as whitespace** (`/\s+/` matches NBSP) — verbatim trailing non-breaking spaces (sheets prose "…forsikringer: &nbsp;") wrap to an extra line in the prototype but not on EDS (24 px at 360). Worked around in `encoders/theme.mjs` (`keepNbsp` on the sheets only). A lib-level fix (`/[ \t\r\n]+/` instead of `\s+`) would make every family verbatim.
- **Core `cardRows` drops content the migrate workers emit inside `li.card`**: a second `h3.card-title` ("Ung"), `p.cta-row` pills, a `div.prose` lede between the section title and the list, `ul.link-cards` / `ul.quick-links` / `.cols.cols-N` vocabulary, `grid-N` column counts, `media-right`. All handled in `encoders/theme.mjs` (`itemRows`/`rail`/`cols`/`quickLinks`/`themeSplit`) and shared to market-landing; other families whose siblings were migrated with the same vocabulary will need the same — consider promoting `rail`/`cols`/`quick-links` to encoders.mjs.
- **Core `splitMedia` ignores `media-right`** (order taken from the DOM, which is media-first) and gives SVG illustrations the `photo` 3:2 crop. Handled per family via the `text-first` swap and the `illustration` variant (`columns-theme.css`). Note: do NOT name a block variant `illu` — styles.css `.illu { width: 72px; height: 72px }` matches the block div (found the hard way: the whole columns block collapsed to 72 px).
- **Rasterised DA illustrations lose their illustration/photo distinction** — in `columns (cols)` cells the CSS can only sniff `img[src$=".svg"]`; big SVGs rasterised to DA PNGs (bedrift: bonde-ku-landbruk-mork, folk-med-sportsutstyr) render with the photo mask. A lib hook (e.g. `imgHtml` emitting illustrations inside a `<p>` with a sibling marker, or an `illustration` variant on the block) would fix it; low priority (siblings, no pixel gate).

## om-oss / markedsnytt-listing — E3
- **scripts.js `buildMediaAutoBlocks` (edited, small):** it moved only the `<a>` into the auto-blocked `embed`/`video` block and discarded the authored `<p>`, so every media link was DEAD in the EW probe (markedsnytt: 3 dead → 0 after). Now the paragraph is moved (`buildBlock(name, { elems: [p] })` inserted at the paragraph's position). Please keep when touching the runtime; the `video` block already expected the paragraph (`a.closest('p')`).
- **blocks/table/table.js (edited, one line):** `row-headers` accepted as an alias of `directory` for the first-column `<th scope="row">` (om-oss selskapsinformasjon needs row headers without the kontakt directory styling). The hub worker owns the block's look; the canon `.table-wrap` Frost-30 header band is not applied on the om-oss table (eyeball-only sibling, accepted).
- **lib.mjs `inline()` (request):** `<span>tag</span><time>date</time>` flattens to `Nyheter29. juni 2026` (no space) — visible in the theme worker's privat.html news meta too. Suggest a space between adjacent inline element runs inside `.meta`. Worked around in `encoders/markedsnytt-listing.mjs` (meta authored as `<em>tag date</em>`).
- **rasterName / serve.mjs (request):** `Innovasjons%20workshop%202%20lys%20bakgrunn.svg` rasterises to a PNG whose file name keeps the literal `%20`; DA serves it (200 with token) but the emulation decodes `%20` before the raster lookup → broken image on /nb/bank/om-oss/jobb-og-karriere in the emulation only. Suggest `decodeURIComponent` + slugify in `rasterName` (and re-upload) or a matching decode in `serve.mjs`.
- **Group CSS order (FYI):** `blocks/<block>/<block>-<group>.css` is `@import`ed BEFORE the core rules, so a group variant with the same specificity as a core rule loses (e.g. `.cards li.art-card` vs `.cards li.card`). Variants must out-specify (`.cards li.card.art-card`) — worth a line in the brief.
- **Variant naming (FYI):** a block variant token that equals a canon compound class collapses the block (`illu` → `.illu { width: 72px }` shrank `columns` to 72 px). Renamed to `spot`.
2. **`lib.inline()` collapses non-breaking spaces.** `out.replace(/\s+/g, ' ')` turns U+00A0 into a plain space, so captured "1&#160;500 tonn", "CO2&#160;-utslipp" re-wrap: on the news-article archetype at 360 the body lost one line before the first figure. Worked around in `encoders/news-article.mjs` (nbsp → private-use char before serialising, back to `&#160;` after). Suggest `/[ \t\n\r\f]+/g` in `inline()` (and `prose()`'s text-node branch) so every family keeps the author's nbsp.

## product — E5
1. **`.illu` compound collides with block variants named `illu`** (styles.css § shared canon compounds: `.illu { width: 72px; height: 72px; object-fit: contain }`).
   Any block authored with the class token `illu` becomes a 72×72 box: measured on the emulation (lanekalkulator, before rename) `.hero.illu .hero-grid` = 72 px wide,
   `.cards.illu ul` grid columns `8px 8px 8px`, images spilling over the next movement. Product renamed its variant to `spot`, but four other documents still
   carry the token: `content/nb/bank/bedrift.html`, `content/nb/bank/bedrift/bedriftsforsikring.html`,
   `content/nb/bank/bedrift/bedriftsforsikring/bransjer/borettslag-sameie.html`, `content/nb/bank/privat/forsikring/hvilke-forsikringer-trenger-man.html`
   (`grep -l 'class="\(hero\|cards\|columns\)[^"]* illu' content/…`). Either scope the compound (`img.illu`) in styles.css or ask those groups to rename.
2. **`blocks/table/` has no group CSS import.** Product's `table (compare row-headers)` rules had to live in `styles/styles-product.css` (block-scoped selectors
   `main .table.compare …`). Please add `@import url("table-product.css")` (and the other groups) to `blocks/table/table.css` like the other blocks; I will move the rules.
3. **`prose-narrow` centres the prose.** `main .section.prose-narrow .default-content-wrapper { max-width: 68ch }` caps the section's `> div`, which carries
   `margin-inline: auto` → the 68ch column floats to the middle (bankid "Avtalevilkår for BankID" measured at x≈380 px on 1440). The canon `.prose-block`
   is start-aligned. Product added `prose-start` (`.default-content-wrapper > * { max-width: 68ch }`); consider making that the core behaviour.
4. **Core `faq` keeps the canon's hidden per-question article link as a visible paragraph** (`a.faq-link` → `<p><a>Question?</a></p>` at the end of every
   answer — 39 on product siblings, also on the LIVE archetype: `grep -c 'kundeservice/lan/' content/nb/bank/privat/lan/boliglan.html`). Product follows the
   core for consistency; a design decision (drop / relabel "Les hele svaret") should be taken once, in encoders.mjs.

## hub (category-hub · kundeservice-hub · tool · utility) — E4
- **delivery-lint `one-cta-per-p` false positive on running text.** The rule fires on ANY `<strong>`/`<em>` in a paragraph with > 1 link
  (`emphasized = /<(strong|em)\b/`), but `decorateButtons` (scripts/scripts.js:128) only buttonizes when the paragraph's text equals the
  link's text — a bold phrase in a sentence with two links (forsikring FAQ: "Hvis du skal <strong>flytte</strong> <a>skadeforsikringen</a>
  til annet forsikringsselskap …, … <a>flytter forsikringen til oss</a>.") ships as plain running text. Suggest testing `strong > a` /
  `em > a` (or `p.textContent === a.textContent`) instead. Worked around locally (`tidyProse` in encoders/category-hub.mjs: run-in bold
  labels become their own paragraph, a bold span around a link is split, and as a last resort the paragraph is split at a sentence boundary).
- **callout p + p rhythm.** `blocks/callout/callout.css` zeroes every paragraph margin inside `.callout-text` (grid gap 8) and gives
  `p.button-wrapper` only 4px, so a pill after a paragraph sits 12px under it; the canon (`.callout p + p` 16 + `.btn` 4, lan prototype
  "Samtykke Altinn": p ends 173 → pill 201) needs 28. Worked around with the hub variant `callout (… cta)` (callout-hub.css: 20px);
  a core `.callout .callout-text p + p.button-wrapper { margin-top: 20px }` would let every family drop the variant.
- **hub CSS import order.** The group files are `@import`ed BEFORE the main block rules, so an equal-specificity group rule loses
  (e.g. `.cards li.small-door` vs `.cards li.tile`, `main .section.tool-intro … h1 + p` vs `main .section.intro … h1 + p`). Every hub
  rule now carries one extra class (`.cards li.tile.small-door`, `main .section.intro.tool-intro`). Importing the group files AFTER the
  main rules (or documenting the specificity contract in the brief) would spare the next worker the same detour.

## story — news-listing · campaign-landing (round 01)
1. **`/footer-frontend-om-oss-2` is 12 px taller than the nyheter / hjemme prototype footers at 360.** Gate at 360: `nb-bank-om-oss-nyheter-html` footer h 778 (proto) / 790 (EDS), crop 97.67 %, Δh −13 with the body identical (footer y 11273 / 11274); `nb-bank-om-oss-hjemme-html` footer 778 / 790, crop 98.23 %, Δh −12 with footer y identical (5129 / 5129). The article prototype (`nb-bank-om-oss-nyheter-bankkort-laget-av-resirkulert-plast-html`) renders a 790 px footer, so the same fragment matches that page (100 %). At 1440 all three footers are 422 / 422. One frontend footer document therefore fits the article capture but not the listing / campaign captures (one wrapped line in a footer column). Chrome — not touched; the two gates fail only on this (pixel 2.58 % / 0.23 %, content-diff 0 🔴). Request: a second frontend-om-oss footer variant for those pages in chrome-map, or accept the 12 px.

## story — faq (round 01)
1. **`/footer-bedrift` was 2 px shorter than the prototype footer at 360** (footer h 1462 / 1460, crop 97.7 %) on the first gate run of `nb-bank-bedrift-kundeservice-bm-lan-finansiering-fast-flytende-rente-bedriftslan-html`; on the final run it measured 1462 / 1462 (100 %). Recorded in case it flips again (a lazily loaded footer icon or font swap).

## story — news-article (round 01)
1. **Base `hero.css` phone rule `.hero .hero-media img { position: absolute; inset: 0 }` (≤ 767) removes the photo from flow.** The article hero photo sits above a caption strip inside the same figure card; absolute positioning collapsed the figure to the caption height (64 px) at 360. Worked around in `blocks/hero/hero-story.css` (`body.news-article .hero.article .hero-media img { position: static }`). Consider scoping the base rule to `.hero-media:not(:has(.caption-wrap))`.
2. **`blocks/video/video.css` `.video video { aspect-ratio: 3/2 }` also matches the hero's `video` variant** (`div.hero.video`) — the campaign hero video grew to 3:2 (row 509 instead of 480). Worked around in hero-story.css (`aspect-ratio: auto`). A block-scoped selector (`.video.block video` or `.video > .video-fig video`) would avoid the collision.

## om-oss — presse (theme/omoss worker, 2026-09-15)
- **Footer seam on om-oss pages.** The prototype footer sits `--section-padding` (64 / 48 at ≤ 767) below the last movement: on privat pages EDS gets that from `footer section.contact { margin-top: 64px }`, but `/footer-frontend-om-oss` (no contact band) starts flush — measured on `/nb/bank/om-oss/presse` at 1440: feedback ends 2463, EDS footer top 2463, proto footer top 2527 (`footer { margin-top: 64px }` in the proto chrome); at 360 the gap is 48. Every om-oss page is therefore 64 px short (gate Δh −64 with all movements matching). Suggested chrome fix: `.footer .site-footer:not(:has(.contact))` / the om-oss footer variant carries `margin-top: var(--section-padding)`. Worked around locally in `styles/styles-omoss.css` (`body.om-oss main > .section:last-child` pays the padding — marked WORKAROUND, remove when fixed).
- **Feedback strip leading.** `blocks/feedback/feedback.css` sets the question `line-height: 1.17`; the canon `.feedback h2` inherits the heading leading 1.05, so a two-line question at 360 is 56 px vs 50 (Δh +6 on every page whose question wraps; the home page's 360 Δh 5 is this). Scoped fix in `feedback-theme.css` / `feedback-omoss.css`; suggest moving `line-height: 1.05` into the base.
- **Hero back link double margin.** `.hero .hero-text .backlink-row { margin: 0 0 20px }` also matches the breadcrumbs block's inner `nav.backlink-row`, so the back-link row is 60 px instead of 40 (measured on the theme hero). Scoped fix in `hero-theme.css` (`.backlink-row .backlink-row { margin: 0 }`); suggest `> .backlink-row` in the base.

## hub — round 01 (category-hub · kundeservice-hub · tool · utility) — E4
- **gate.mjs `measure` pass reads the prototype before its lazy SVG loads (footer crop misaligned).** lan at 360: the chrome `measure` pass reports proto doc 8261 / footer y 6724 while the stitched proto-360.png is 8377 tall (the `dame-gar-smiler.svg` promo illustration, `loading="lazy"` with 160×120 attributes, renders 120×90 before load and 120×206 after; the 40 ms scroll loop does not wait for it). The EDS side is deterministic (8377 in both passes since `aspect-ratio: revert-layer` reserves the raster's attribute box), so `--y 6724 --y-b 6841` compares the wrong proto band → 81.57 %. Cropping both footers at their true offset gives 100 % (`crop-compare proto-360.png eds-360.png --y 6841 --y-b 6841 --height 1536` → diffPixels 0). Request: in `measure`, await `img.decode()` for every image (or reuse the stitched capture's footer offset) before reading the footer rect.
- **`/nav` header at 360 on the kundeservice hub: 97.1 %** (`stardust/rollout/gates/nb-bank-privat-kundeservice-html/chrome-header-360.png` — the "Logg inn" pill region differs; lan / sperre-kort / kontakt / home are 100 % with the same header). Chrome — not touched; please compare the proto's kundeservice header at 360 with the header block.
- **`aspect-ratio: auto` on an authored image discards the UA attribute ratio.** The emulation (serve.mjs) writes the raster's `width`/`height` on DA-media images, but any author `aspect-ratio` (even `auto`) beats the UA `img[width][height]` hint, so a lazy illustration has a 0 px box until it loads and every section below it shifts (lan 360: −90 / +116 px between passes). `aspect-ratio: revert-layer` restores the attribute ratio; a base-level `img[width][height] { aspect-ratio: revert-layer }` next to `.card-image { aspect-ratio: 16/9 }` would spare every group the trap.
- **Variant tokens that collide with canon compounds (FYI, again):** `title` (`.title` = t-title type role) and `small` (`.small` = 14 px) shrank the hub hero / small doors — renamed `text-only` and `icons spot`. Together with `illu` (product/theme) that is three collisions; a lint on block variant tokens vs styles.css single-class selectors would catch them.
- **Group CSS import order (repeat):** group files are `@import`ed before the base rules; every hub variant now carries the template body scope `:is(body.category-hub, body.kundeservice-hub, body.tool, body.utility)` (per the coordinator) which also lifts specificity. Importing group files after the base rules would remove the need for the extra class in the other groups.
- **hero.js `split-h1` (edited, one line):** `ask` / `portrait` no longer lift the h1 beside the photo (round-01 heroes keep the h1 inside the text card); the round-0 layout is opt-in via the `split-h1` class. Home and boliglån re-gated after the edit (home 1440 0.92 % / 360 3.7 %, boliglån 1440 1.43 %).
- **lib.inline() nbsp (repeat, evidence):** sperre-kort misuse callout at 360 wrapped one line shorter (−20 px) because `&#160;` collapsed to a space; worked around with `keepNbsp()` in encoders/category-hub.mjs (private-use placeholder around inline/prose, restored as `&#160;`).
- **Prototype: `.faq summary { font-size: var(--lead) }` ≤ 767 (kundeservice) vs 24 px on the other hub FAQs** — the hub accordion now uses 17 px/1.2 on phones for all hub bodies (`accordion-hub.css`); if the other families' prototypes keep 24 px the token should move to the kundeservice-hub body only.
