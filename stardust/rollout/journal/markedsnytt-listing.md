# markedsnytt-listing — EDS journal (E3)

## Archetype: markedsnytt (nb-bank-privat-sparing-markedsnytt-html) — PASS
- Blocks: breadcrumbs + hero `listing` (text 5 / photo 7, tops aligned, h1 14ch, backlink 24px above); cards `articles` (flat photo cards, unlinked title, chevron "Les saken her" as whole-card click; `flush-top`); cards `news cols-3`; **embed** (NEW: canon `.video-frame`, click-to-load youtube-nocookie player; auto-blocked from the bare YouTube link in default content); columns `expert` (260px portrait/spot column with caption | text; the caption is MOVED via the additive `col-caption` slot in columns.js); columns `text` (two running-text columns, links stay in place — additive `text` branch in columns.js); feedback `labels`.
- Section styles added: `head-xl` (48px title → block), `video-row` (one webinar row = section: prose left, embed right; 48px after the head, 64px between rows), `fineprint` (title-sm h2 over the regulatory columns).
- Decisions: the webinar rows are NOT a columns block — the YouTube link must stay default content to be auto-blocked (D1; buildMediaAutoBlocks skips `.columns`), so each row is its own section on the same paper. The embed does not distinguish "runtime-injected" from captured frames: both render the linked frame until clicked (matches the proto at load).
- Rhythm traps met: `li + li` 8px inside the webinar list, the external-link glyph makes the pill wrap at 360 (glyph added via CSS mask on off-site hrefs), `.articles` keeps 24px top padding at ≤640, news meta `span+time` flatten (authored as `<em>tag date</em>`).
- Gate: 1440 0.08 % / Δh 0 · 360 0.32 % / Δh 0 · header 99.96/99.66 · footer 99.89/100 · content-diff 0 🔴 (26 🟡, 12 🟠 — bank-router ctas inside main) · EW 62/63 editable, dead 0, exempt 1 (fragment).
