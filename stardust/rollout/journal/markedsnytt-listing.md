# markedsnytt-listing — EDS journal (round 01, 2026-09-15)

- **Hero** = `breadcrumbs` + `hero listing` (the core productHero shape, photo object-position 50 % 30 %); the featured articles continue the bento: `cards articles grid-3` under the `bento-join` section style (6 px, not a movement gap), "Les saken her" as the card link with the canon chevron and a `::before` cover.
- **Webinar rows — David's Model decided the shape.** A first pass put the YouTube link in a `columns split video` cell (with a small additive video-card wrapper in columns.js); `davids-model-lint` flags a media URL inside a block as 🔴 D1, so the rows went back to default content: one `video-row` section per row = text card 5 (default-content wrapper on Sand-70) + the auto-blocked `embed` as the Frost video card 7 (click-to-load player, dynamics #16 — better than the proto's static link). The columns.js change was reverted; the base is untouched. Row gap is 12 px (canon `.rows` gap + `.bento + .bento`).
- **Expert rows** = `columns split expert` (Syrin tile 3 with the 160 px round portrait + caption, text card 9). The illustration row is told apart from portraits by `img:not([src$=".jpg"])` (illustrations are SVG/PNG). Mobile paddings had to be restated inside the media queries because the variant's desktop padding outranks the base mobile rule.
- **Regulatory** = `cards regulatory grid-2` under `fineprint` (left-aligned h2-s); bold run-in labels become block lines, document links sit 8 px under their paragraph.
- **Rhythm traps.** `li + li` 8 px on top of the 8 px list gap (16 in the canon `.prose`); `.btn-inline` 40 px vs the base 44 px cta link; off-site pills 46 px because of the external glyph (reproduced via a scoped `::after`); news meta `.small` leading 1.43 (1 px per card); mobile `.h2-l` leading 1.12 on hero/section titles.
- **Scoping.** All rules under `body.markedsnytt-listing`; single-page family, no siblings.
