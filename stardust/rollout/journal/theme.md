# theme — EDS journal (round 01, 2026-09-15)

- **Hero as a block.** The re-crafted intro is one Frost card spanning 12 (back link · h1 · lead). Authored as `breadcrumbs` + `hero theme` with a single text cell: hero.js already moves the breadcrumbs into the card and marks a picture-less hero `hero-text-only`; `hero-theme.css` only drops the reserved 440 px row and sets the 48/48/56 padding. Walker siblings without a `.hero-card` keep the prose intro (`page-title` falls back to the round-0 path).
- **Sheets are cards.** `ul.bento.sheets > li.card` → `cards sheets spot grid-2` (`spot` = illustration in the body). The arrow-link list with descriptions survives as `<li><a>label</a> description</li>`; the block classes the anchor `.arrow`, the description is a bare text node that `li { display: grid }` puts on its own row. The compare link is default content with the `compare-link` section style (arrow look).
- **Band cards.** Tip → `callout band` (icon · h2+p · action, spanning 12 — the bulb stays block chrome, the pill gets the cover); claim row → default content with the `claim` section style painting the Frost band (D1: a prose composition). 360 stacks both, pills full-width, `white-space: normal`.
- **Split rows.** Resident = `columns split text-first resident` (text 7 + Syrin illustration tile 5), prevention = `… prevention` (text 5 + photo 7). The base `lead-wrap` makes the first paragraph 18 px — the theme's cards carry body copy, so the variants restore 16 px / 68ch / p+p 16. Both rows take the media's natural height (the base now keeps photo cells in flow on desktop).
- **Traps met.** `&nbsp;` runs in the captured copy change the 360 wrapping (−1 line) → `keepNbsp` now also runs on the split modules. The proto's mobile leading (`.h2-l` 1.12 / `.h2-m` 1.14 at ≤ 767) explains −8/−10/−7 px on hero, sheets and split headings. Base rules found while gating and worked around in group CSS (requests filed): breadcrumbs nested in the hero double the 20 px back-link margin; feedback question leading 1.17 vs canon 1.05 (+6 at 360 when it wraps).
- **Scoping.** Every rule is under `body.theme` (template metadata → body class); home and boliglån gates unchanged (0.92 %/Δh 6 · 3.7 %/Δh 2 · 1.43 %/Δh 3).
- **Siblings.** bedriftsforsikring, netthandel-kredittkort, hvilke-forsikringer converted through the shared walker with 0 gaps and 0 lint red; bedriftsforsikring content-diff 0 🔴, screenshots at 1440/360 show every module (cards, bransje photo grid, kontakt promo, FAQ, cobranding, feedback). Observation for the walker owner (unchanged from the recraft report): the bransje card rail still sits on a `paper-sand` movement.
