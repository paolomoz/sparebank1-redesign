# theme + market-landing — EDS journal (Worker E2, group `theme`)

## 2026-09-14/15 — archetypes gated, siblings converted
- Encoders: `stardust/scripts/eds/encoders/theme.mjs` (theme family + shared new keys `cta-row`, `quick-links`) and `market-landing.mjs` (re-exports `contentColumns`, `rail`, `themeSplit` from theme.mjs for its own family).
- Block choices (theme archetype): `columns (sheets)` for the two comparison sheets — one row, one cell per sheet [illustration, h2, prose, link list]; the sheets subgrid-align their 4 parts like the prototype via a 3-line guarded addition to `columns.js` (`sheets`: body paragraphs after the heading share one `.prose-wrap`). The trailing compare link is default content (styled after the block). `cards (papers)` for the 3 Sand-30 recommended cards (`tips` = 4 flat photo cards, different treatment). `cta-row` (claim) = default content [icon p, title p, pill p] with the new section style `claim` (styles-theme.css) — a prose composition, not a block (D1). `callout (frost offer)`: `offer` = callout ending in a pill, restores the canon `p + p` rhythm. Split columns get `resident` (illustration right, flush-top) / `prevention` (1fr 1fr) family classes as the core encoder already emits.
- Block choices (market landing): core `hero (campaign)`, `cards (tiles|news)`, `columns (promo promo-2)`; new `columns (index)` for OM OSS/SNARVEIER (one row, two cells [h2, link list], `hairline-top`). News meta authored as `<em>tag</em> <em>date</em>` (two flex items like the canon `.meta`); cards.js `isMeta` (another worker's generalisation) already accepts several `<em>` runs.
- Rhythm traps met: `p + p` 16 px lost in callout / hero note / promo / claim row (restored per variant); `@import`ed group CSS sits BEFORE the core rules → equal-specificity overrides lose (fixed with an extra class / `main` prefix; the `main` prefix in turn beat the core ≤1023 stacking rule and needed its own media rule); the core callout column flip at 640 has no canon counterpart; the tile title line-height 1.2 vs canon 1.15; NBSP collapse in `lib.inline`.
- Prototype defect kept for parity (documented in eds-requests.md): 97 px of empty grid tracks under the stacked sheets at ≤767.
- Gate results: theme 1440 0.04 % / Δh 0 · 360 0.05 % / Δh 0, chrome ≥ 99.66 %, content-diff 0 🔴; privat 1440 0.14 % / Δh 0 · 360 0.15 % / Δh 0, chrome ≥ 98.91 %, content-diff 0 🔴 (17 🟡 12 🟠 = the same router/nav class of ambers the product page shows). Product regression: 0.74 % / Δh −1 unchanged.
- Siblings: migrated with a verbatim vocabulary (`cols cols-N`, `ul.link-cards`, `ul.quick-links`, `grid-N`, `media-right`, `details.disclosure`, double titles + CTA in `li.card.choice`, `.prose` lede in a rail) that the core rail silently drops or mis-grids. Added an order-preserving `itemRows`/`rail`, a `cols` classifier (all single-card columns [+ trailing CTA column] → `cards choices grid-N`; illustration+heading columns → `cards tiles grid-N` with the disclosure flattened to a visible link list; otherwise `columns cols cols-N` prose cells), `cards (quick-links|link-cards)`, `grid-N` modifiers, `media-right` → `text-first`, SVG media → `illustration`.
- Lesson: never name a variant after a canon compound class (`illu` → the block itself became 72 × 72).
- Rasterise: ran `rasterise-svg.mjs` once (default set = every eds-log entry, 36 uploaded / 10 unchanged / 0 failed; includes other families' assets — idempotent).

## Open questions
- Should the `cols` mixed branch (bedrift "Starte og drive bedrift" + two illustrated rows) rather be flattened to `cards choices grid-3`? Kept as `columns cols-2` prose cells to preserve the asymmetric editorial layout; the rasterised illustrations render with the photo mask there (see eds-requests).
- `details.disclosure` → visible link list: the collapsed behaviour is not reproduced (no nested widget in a cell); acceptable per D5, flagged in the eds-log notes.

## 2026-09-15 — final
- Final gates: theme 1440 0.04 % / Δh 0 · 360 0.35 % / Δh 0 (header 99.96/99.66, footer 99.89/100), content-diff 0 🔴 0 🟡; privat 1440 0.12 % / Δh 0 · 360 0.15 % / Δh 0 (footer 98.91 at 360 — same as the product page's class of chrome diff), content-diff 0 🔴. Product regression 0.74 % / Δh −1 (unchanged from the reference gate) — evidence in `stardust/rollout/gates/nb-bank-privat-lan-boliglan-html-regress-theme/`.
- The `illu` variant collision (styles.css `.illu`) collapsed every illustration split to 72 px and pushed the resident Δh to −328; renamed to `illustration`. Its later-in-file width then outranked the resident's ≤1023 300 px (Δh +20 at 360) — `main` prefix on the mobile rule.
- Siblings after the fix: no dropped module, no collapsed grid on any of the five (section probes; stitch-shots of bedriftsforsikring + bedrift at 1440/360 eyeballed). Tiles with a description at 360 needed `.card-text { grid-column: 1 / -1 }`.
- Progress: `stardust/rollout/eds-progress/{theme,market-landing}.json`. Migrated-page server left running on :8821 for the other workers' sibling diffs.
