# om-oss — EDS journal (round 01, 2026-09-15)

- **Presse hero** = `hero om-oss`: the photo card leads (7) and the Frost text card follows (5); one variant rule swaps the base columns, mobile restacks photo-first.
- **Adviser cards** = `cards advisers grid-3` / `grid-2` (span from the captured list length). The block's flat body (portrait, title, role, phone, mail) becomes the horizontal card through a two-column grid (`112px 1fr`, portrait spanning four rows) — no JS wrapper needed. Phone/mail keep the canon 8 px steps; "e-post" is the cover link. The section head is default content with the `contacts` style (h2-l · lead with the number · small muted note; the two rails 6 px apart).
- **Lokale** = `columns promo lokale` (Syrin tile, headline-sm heading, 12 px rhythm, 260 min-height); **Presserom** = `columns split text-first presserom` (photo takes its natural height; inline "Velg bank" is the 40 px `.btn-inline`; off-site pills carry the canon external glyph via a scoped `::after`).
- **Chrome finding.** With every movement matching, presse was 64 px short: the proto footer sits `--section-padding` below the last movement and `/footer-frontend-om-oss` (no contact band) starts flush — the privat footer gets the seam from `.contact { margin-top: 64px }`. Filed in `eds-requests.md`; worked around in `styles-omoss.css` (last om-oss section pays the padding, marked WORKAROUND).
- **Scoping.** All rules under `body.om-oss`; the feedback leading fix is shared with markedsnytt under `body.markedsnytt-listing`.
- **Siblings.** Seven walker pages converted (0 gaps, 0 lint red); samfunnsansvar content-diff: "content + roles match"; screenshots at 1440/360: hero bento, three icon cards, illustration panel, text, photo, feedback — nothing dropped, cards stack at 360.
