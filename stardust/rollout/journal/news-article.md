# news-article — EDS conversion journal (Worker C, story group)

- 2026-09-15 · `article-header` → `hero (article)` [photo + <em>caption</em>][h1, <em>tag</em><em>date</em>, lead]; share buttons are block chrome (@ew-exempt aria-labels). `article-body` → one section, style `article` (68ch prose grid beside a sticky `cards (rail)` spanning every body row): body as default content, fact box → `callout (fact)`, pull quote → `callout (quote)`, text-and-image → `columns (split article-split)`, story columns → `columns (article-cols)`, mp4 → plain link (auto-blocked `video`).
- hero.js additive branches (meta paragraph = <em> only; caption in the media cell; share chrome; mp4 media) were overwritten once by the hub group's rewrite (00:03) and re-applied on top of their `ask`/`portrait` code — grep-verified.
- Body matches the prototype at both widths (footer y identical); the gate FAILS only on the lead's frontend footer (+252 px, 4th column of legal links) — eds-requests story #1. `lib.inline()` collapses U+00A0 → one-line re-wrap at 360; kept locally (eds-requests story #2).
- Lint 🟡 D1 callout, D3 rail rows (heading rows + story rows) — justified in notes. 9 siblings converted, gaps 0, 0 🔴/0 P0-P1; sampled `bamses-historie-traktorulykken`: 35/35 text nodes, 0 🔴. Eyeball 1440/360 clean (the rail repeats only in the stitched capture because it is sticky — as in the prototype).

## Round 01 (2026-09-15, story group)
- `article-header` → `hero (article)`: Sand-70 text card 5 + photo card 7 with the caption as a Sand-70 strip under the photo (hero.js `caption-wrap`), meta line `<em>tag</em> <em>date</em>` painted with the "·" separator, share buttons block chrome. `article-body` → style `article`: 68ch default-content column LEFT-aligned in the container (the prototype's `.body-grid`; the base wrapper centres — `margin-inline: max(gutter, (100% − max-width)/2 + gutter) auto`), fact box `callout (fact)` (Sand-70, no icon, li gap 16), pull quote `callout (quote)` as ONE cell (callout.js moves only the first cell; grid areas paint portrait/quote/who). `card-rail` (family override, style `related`) → h2 + `cards (news rail)` with railSpans painted from `ul[data-items]` (wide photo-left remainder rows) + "Relaterte tema" h2 + link list painted as 6 px badges.
- Traps: base hero.css phone rule makes `.hero-media img` absolute (collapsed the captioned figure to 64 px) — scoped `position: static`; the frontend prototypes add `footer[data-canon]{margin-top:64}` on top of the last movement's padding → `body.<template> main > .section:last-child { padding-bottom: 2 × section-padding }`; figure rhythm 48/48 (24 at ≤ 767) with the h2 keeping 48 after a captioned figure.
- Gate: 1440 0.03 % / Δh 0 · 360 0.05 % / Δh 0 · chrome ≥ 99.84 % · content-diff 0 🔴. Lint 0 🔴 / 1 🟡 (callout fact), delivery 0 P0/P1 (7 P2 cross-origin img), EW 28/28. 9 siblings converted (gaps 0, lint 0 🔴); sampled `bamses-historie-traktorulykken` 35/35 nodes 0 🔴, eyeballed 1440/360 (story layout: hero photo, figures, 3 + 2-wide rail, badge).
