# campaign-landing — EDS conversion journal (Worker C, story group)

- 2026-09-15 · `video-hero` → `hero (video)` [mp4 link][h1] (hero.js builds <video controls muted loop preload=metadata>; the link stays in a hidden wrapper); `story-text` → default content, style `chapter` (+ `paper-sand`, + `lead-first` only where the captured first paragraph is the lead); `split-media` → `columns (split slides)` / `columns (split closing)`; `video` → plain mp4 link auto-blocked to the new `video` block + caption paragraph (icon + <em>title</em>), style `film`.
- New block `blocks/video/{video.js,video.css}` (3:2, 48 px corner, 16:9 ≤ 1023; media stays on www.sparebank1.no).
- Traps met: a spanning h2 (`grid-row: 1 / span 30`) multiplies the wrapper row-gap — chapter uses row-gap 0 + margins; the film width is 7/12 of the container, not the section; slide gap 48 at ≤ 640.
- Body identical to the prototype at both widths; gate FAILS only on the lead's frontend footer — eds-requests story #1. EW 25/25 editable. Lint 0 🔴 (1 🟡 D4: verify video-ikon.svg is pure vector), delivery 0 P0-P1. Captured in-page anchors (#kontakt, #blikunde, #samfunn) → section ids `slides` and `samfunn` only (per-row anchors are not authorable in one block).

## Round 01 (2026-09-15, story group)
- `video-hero` → `hero (video)` (Frost-30 statement card with the display h1 + the loop filling the 7-col card); `story-text` → `columns (split chapter dark|frost [reverse] [lead-first])` — title tile 5 + Sand-70 prose card 7, mirrored for chapter 2; slides → `cards (slides grid-3)` with 1:1 photos and the two CTAs wrapping like the prototype's flex `.actions`; `video` → `video (film)` with a SECOND authored cell (icon + <em>title</em>) — additive video.js: the caption cell is MOVED into `figcaption.video-cap` (video card 8 + Syrin-30 caption card 4, painted from styles-story.css since video.css has no group import); closing → `columns (split closing)` (photo 5 + Fjell tile 7).
- Traps: `video.css` `.video video { aspect-ratio: 3/2 }` also hits `div.hero.video` (row grew 480 → 509) — neutralised; the prototype's `.prose > p + p` (16) outranks its `.actions` (24) so the chapter CTA sits 16 below the text; the scoped desktop `grid-template-rows: minmax(360px, auto)` needs an equally scoped tablet reset (multi-line selectors must be scoped too).
- Gate: 1440 0.03 % / Δh 0 · 360 0.23 % / Δh −12 — the −12 is the `/footer-frontend-om-oss-2` height (778/790, footer y 5129/5129) → request filed. Lint 0 🔴 / 1 🟡 (D4 video-ikon.svg), delivery 0 P0/P1, EW 25/25. Home regression 1440 0.92 % / Δh 6 (recorded 1.05 / 6), boliglån 1440 1.43 % / Δh 3 — pass. No siblings.
